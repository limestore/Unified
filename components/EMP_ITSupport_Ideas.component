<apex:component controller="EMP_ITSupport_Ideas_Controller">            
    <apex:dataList var="a" value="{!ideaList}" id="list" styleClass="itSupport_ul">
        <a href="{!communityUrl+'/ideas/viewIdea.apexp?id='+a.id}" target="_blank">{!a.title}</a>              
    </apex:dataList>
   
    <div class="dvShowMore">
        <a href="{!communityUrl+'/ideas/ideaList.apexp'}" target="_blank">Create/Search an Idea</a>
    </div>
  
</apex:component>